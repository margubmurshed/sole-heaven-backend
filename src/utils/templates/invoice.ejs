<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Booking Invoice</title>
  <style>
    body { font-family: Arial, sans-serif; font-size: 14px; color: #333; margin: 0; padding: 0; }
    .container { width: 700px; margin: 0 auto; padding: 20px; }
    .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
    .logo img { width: 120px; }
    .company-info { text-align: right; font-size: 12px; color: #555; }
    h1 { margin: 20px 0; font-size: 28px; color: #111; }
    .billing-info, .invoice-meta { margin-bottom: 20px; }
    table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
    table th, table td { border: 1px solid #ddd; padding: 10px; text-align: left; }
    table th { background-color: #f5f5f5; }
    .total { text-align: right; font-weight: bold; }
    .footer { text-align: center; font-size: 12px; color: #777; margin-top: 30px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">
        <img src="https://i.ibb.co.com/99n30ccq/logo.png" alt="PH Tour Management System">
      </div>
      <div class="company-info">
        PH Tour Management System<br>
        Dhaka, Bangladesh<br>
        Phone: +8801988474979<br>
        www.phtms.com
      </div>
    </div>

    <h1>Invoice</h1>

    <div class="billing-info">
      <strong>Billed To:</strong><br>
      <%= userName %>
    </div>

    <div class="invoice-meta">
      <strong>Invoice Details:</strong><br>
      Transaction ID: <%= transactionID %><br>
      Booking Date: <%= new Date(bookingDate).toLocaleDateString('en-GB') %>
    </div>

    <table>
      <thead>
        <tr>
          <th>Description</th>
          <th>Guests</th>
          <th>Price (BDT)</th>
          <th>Amount (BDT)</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= tourTitle %></td>
          <td><%= guestCount %></td>
          <td><%= (totalAmount / guestCount).toLocaleString('en-BD', { style: 'currency', currency: 'BDT' }) %></td>
          <td><%= totalAmount.toLocaleString('en-BD', { style: 'currency', currency: 'BDT' }) %></td>
        </tr>
      </tbody>
    </table>

    <div class="total">
      Total: <%= totalAmount.toLocaleString('en-BD', { style: 'currency', currency: 'BDT' }) %>
    </div>

    <div class="footer">
      Thank you for booking with PH Tour Management System!
    </div>
  </div>
</body>
</html>
